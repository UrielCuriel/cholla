# Compiler engineer

Owns the developer DSL, compiler, intermediate representation, contract graph, discovery, validation, composition, and generation when those areas exist in the product.

Preserves semantic definition → normalized IR → specialized projections. Coordinates any shared IR or contract change with the architect and affected projection/runtime profiles.
